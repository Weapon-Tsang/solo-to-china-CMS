# CMS 第二轮修复与生产发布记录

日期：2026-09-12（Asia/Shanghai）  
状态：完成  
运行时代码：`1739675`（`codex/audit-v1.3`）  
应用 / 扩展：`2.0.3`  
内容策略：`3.0`  
数据库 Schema：`58`

## 完成范围

- Content 只显示已进入真实生产入口的记录；等待证据、系统重算和内部机会不再占位。
- 推荐收件箱由内部机会投影生成，区分系统处理缺口与编辑证据缺口，并校验当前采集、Experience、诊断和策略版本。
- 同义意图按规范键合并但不删除内部记录；批准或忽略一个规范意图后，不会由其重复变体再次占位。
- 历史失败只有在存在当前、有效且标记为安全的修复规则时才允许重新推荐；研究层保留，失败稿件不复用。
- 系统健康只保留认证、数据库、网络、配额、超时、集成和 WordPress 等基础设施故障；媒体修复、Experience、推荐重算和历史失败治理进入聚合维护卡。
- 媒体修复完成后失效旧 Experience 与推荐投影，并依次重算 Experience、来源语义诊断、知识和机会。
- 删除四条硬编码联盟 seed 配置；数据库中已到终态的旧记录保留审计历史，若存在活动旧 seed 则迁移为已跳过，联盟队列默认只显示真实活动缺口。
- CMS 与扩展中的原始内部状态词替换为运营可读中文；扩展继续明确区分增量同步、缺失修复和完整核验。

## 生产数据前后对照

| 指标 | 发布前 2.0.2 | 发布后 2.0.3 | 解释 |
|---|---:|---:|---|
| 来源 | 74（全部 processed） | 74（全部 processed） | 来源与研究数据未删除 |
| 媒体 / 已存原件 | 1,311 / 362 | 1,311 / 362 | 949 个原件仍需浏览器修复 |
| Experience 行状态 | 74 succeeded | 74 succeeded；其中当前且非降级 2，受原件阻塞 72 | 新版不再把旧或降级成功误报为当前可用 |
| 推荐主计数 | 822 个旧“待处理” | 822 个内部机会；0 个可行动；822 个系统处理缺口 | 缺原件的机会不再要求人工决策 |
| Content 显示 | 6 个已批准候选 | 2 个真实进入生产的记录 | 等待证据的批准不再占位 |
| 系统健康 | 999 组 / 1,129 条广义异常 | 6 组 / 6 条基础设施故障 | 维护和内容问题已分离 |
| 联盟任务 | 4 条已完成历史；活动计数 0 | 4 条已完成历史；活动计数 0，默认界面显示 0 | seed 不再生成或默认展示，已完成审计记录不改写 |

发布后聚合维护状态：

- 媒体原件：总计 1,311，完成 362，浏览器阻塞 949。
- Experience：总计 74，当前完成 2，可立即排队 0，受媒体阻塞 72。
- 推荐校准：内部机会 822，可行动 0，系统处理缺口 822，旧版本/不可重试项 243。
- 历史失败治理：Failure Lesson 15，当前安全可重试 0，阻塞 15。

## 回填预检

只执行 dry-run；没有执行付费模型调用、批量内容生产或数据删除。

- 媒体：`media_backfill_de7ee30db43b426e8576049106329e74`；扫描 949，可服务器恢复 0，需浏览器修复 949，不可用 0。
- Experience：`backfill_8fd7ee768c7f4271ba2380bea9d41853`；可立即回填 0，受媒体阻塞 72。
- 推荐：`backfill_e2efded1410a44b292335619040b797c`；7 个目的地，74 个诊断，机会 849 -> 849，未执行重建。
- 历史失败清理：`backfill_9fd52425ead2427ea44747e391b3a3c7`；扫描 0，安全回滚 0，迁移复核 0。

## 测试与发布证据

- 最终离线发布门禁：49 个强制检查通过，0 失败；固定前端 Contract `1.3.0` 在提交 `f44ce1092ced93dfb47d9b3eae83d0d5e4b97086` 通过。
- 单元与集成测试全量通过；Vite 构建、Node 语法、服务边界、迁移链、SQLite 完整性、备份恢复演练和 HTTP smoke 全部通过。
- 最终 Cloud Build：`5529c54a-bde9-4a40-b52e-7b8cda226e79`。
- 最终镜像：`asia-east1-docker.pkg.dev/project-4bcb9146-c37b-43b0-b11/solo-to-china/engine:2.0.3`。
- 镜像摘要：`sha256:bcab141f409bbe353962f34f5e0dab9381c827be6e82d4c80c37185fd1bbda3e`。
- 目标：GCE `solo-to-china-engine`，`asia-east1-b`，项目 `project-4bcb9146-c37b-43b0-b11`。
- 串口日志在 `2026-09-11T18:01:49Z` 记录了持久卷预升级数据库与内容快照步骤；`engine` 和 `cloudflared` 均为 Up，`2026-09-11T18:02:11Z` 记录发布成功。
- Engine health / ready 均为 HTTP 200，应用 `2.0.3`、数据库 ready、策略 `3.0`、Frontend Contract healthy。
- Capture health 为 HTTP 200，Capture 根路径为 HTTP 404，未暴露 CMS。
- 推荐接口在相同 822 条生产机会上的最终顺序探针为 1,368 ms；Dashboard 为 3,960 ms。此前并发诊断样本分别为 26,028 ms 与 11,772 ms；两组采样方式不同，仅用于确认重复读取已消除，不作为 SLA。

## 保留边界与下一步

- 949 个缺失原件必须由已登录小红书的真实 Chrome 会话使用扩展“修复缺失数据”补回；部署不能合法重建这些字节。
- 原件补齐后系统会自动重算对应 Experience 与下游推荐；在此之前 72 个来源和 822 个机会保持系统处理状态是正确结果。
- 6 个系统健康故障仍需按具体认证、网络或集成错误处理；没有通过隐藏或删除记录制造“全绿”。
- 本次未修改或发布任何 WordPress 正文，也未验证真实 Chrome 捕获、生产 WordPress 渲染、爬虫响应、排名、收录、流量或 AI 引用结果。
