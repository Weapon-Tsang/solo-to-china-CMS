SoloToChina 项目问题清单与完整修复提示词

完整可复制提示词

================================================================

如何在另一个账号使用
推荐方式：将“01_问题清单.txt”和“02_完整修复提示词.txt”上传到新账号的同一任务，发送：
“请读取这两个文件，按总提示词和建议批次执行全部修复及优化，持续更新 repair-progress.md，不要只给计划。”

分批方式：先发送总提示词，再按批次粘贴对应 A/B 条目。若开启全新任务，可上传 ZIP 内“逐项提示词”中的对应 TXT；每个文件都包含仓库、基线、通用约束、问题描述和验收，不需要前一个账号的聊天记录。

节省额度：优先完成批次一至四的 A 类缺陷；B 类是已纳入任务范围的优化，继续在批次五执行。跨账号中断前要求保存 repair-progress.md 和精确下一步，不重复从零审计。

各提示词允许修复所需的代码、测试和迁移；不等于授权生产部署、破坏性数据操作或任意付费请求。


交接日期 2026-09-07
CMS 仓库 https://github.com/Weapon-Tsang/solo-to-china-CMS
前端仓库 https://github.com/Weapon-Tsang/solo-to-china
CMS 审计提交 1a597ecfe2b89ce75f491bcbbf606f9c52d294ca
前端审计提交 ccfab41f25cd2323bfcd049485bdcb44a71df622

重要边界
1. 本交接包是修复任务书，不表示这些问题已被修复。
2. 原审计 CMS 118 项测试通过、静态与构建检查通过、35 项本地 smoke 通过；这些测试没有覆盖所有发现。
3. 已复现包括隔离代码和 mock 行为，不代表真实模型或生产 WordPress 已发生相同错误。
4. 原审计未执行真实付费模型、生产 WordPress/PHP、线上 HTML 和浏览器验收；A02 真实模型兼容性、A17 故障窗口等需在适当环境验证。
5. P1 表示扩大自动生产前优先处理，P2 表示明确的可靠性、安全或质量缺口；未定性为 P0 或已被利用的公网漏洞。
6. 路径均以对应仓库根目录为基准。新账号应重新核对当前 HEAD，不能假定仍是审计版本。


推荐批次
批次一 任务与契约基础：A16 A01 A19 A02 A12
批次二 证据闭环：A18 A03 A04 A09 A14 A13
批次三 最终页面与素材：A06 A05 A07 A08 A10
批次四 交付 SEO 与认证：A17 A11 A15
批次五 优化任务：B01 B02 B03 B04 B05 B06 B07 B08 B09 B10
最终执行总验收提示词。
依赖可以协同推进；未通过前置验收时不要把下游标为完全完成。


总提示词开始

请作为负责落地修复的工程师，依据附件《SoloToChina 项目问题清单与完整修复提示词》修复两个仓库。CMS 为 https://github.com/Weapon-Tsang/solo-to-china-CMS，前端为 https://github.com/Weapon-Tsang/solo-to-china。执行全部 A01 至 A19，以及 B01 至 B10；不要只给方案。先完成依赖明确的本地实现、迁移、回归测试和交接记录，再报告需要外部环境验证的部分。

审计基线：CMS 1a597ecfe2b89ce75f491bcbbf606f9c52d294ca，前端 ccfab41f25cd2323bfcd049485bdcb44a71df622，日期 2026-09-07。先读取 AGENTS.md、当前 Git 状态和最新差异，定位两个仓库；路径在新账号环境中自行发现，不依赖旧用户名。本机原路径为 Documents/ChatGPT 下两个同名目录。若分支已更新，复核每项是否仍存在；已修好的项以证据关闭，不能为了复现倒退代码。保留用户未提交改动。

附件包括原审计报告、隔离复现脚本和结果。原复现脚本断言的是缺陷行为，并非修复后的正确性测试；请在原脚本旁保留审计证据，另将相关样例转成回归测试，明确修复后的相反期望。A02 的真实模型错误、A17 的 WordPress 故障窗口及线上 SEO 输出尚未实测，不可把推断描述为已发生事故。

推荐实施顺序：批次一 A16、A01、A19、A02、A12；批次二 A18、A03、A04、A09、A14、A13；批次三 A06、A05、A07、A08、A10；批次四 A17、A11、A15；批次五 B01 至 B10。允许根据代码依赖微调，并记录原因。每批保持两个仓库可运行、契约兼容及测试通过，不在半完成状态宣布整项完成。

保留既有持久化队列、Research 与 Commercial 边界、明确文章机会审批、WordPress draft-only 和非草稿保护。不得用关闭校验、清空历史数据、删除失败记录、把所有来源直接标官方或把模型提示词当事实证明来修复。接受项目既有素材授权政策；修复素材内容匹配与追溯，不重复创建授权审批。

版本、证据、媒体和任务状态变更要设计迁移与历史记录兼容。历史 QA 不能自动视为新版本合格，缺少证据的旧数据进入待复核状态。所有测试使用隔离数据库、mock 或明确测试环境；不要让测试访问生产库、修改生产 WordPress、发送商业事件或批量调用付费模型。现有凭据如已获授权可用于必要的最小验证；没有凭据时实现测试工具并列为待验证，不伪造结果。

为每项在 docs/audit/repair-progress.md 记录状态、当前证据、修改文件、测试结果、迁移影响和剩余工作。状态使用 待复核／已确认／实现中／已实现待外部验证／已验证完成／已不存在。额度或上下文不足时保存当前分支、未提交内容、下一步精确命令；下一轮从该文件恢复，不重新审计全部仓库。

最终执行 CMS npm test、npm run check、npm run test:smoke，以及前端项目现有的契约、运行时、PHP 和页面验证。增加跨仓库真实生成契约样例与故障注入测试；用最终 HTML 验证可见内容、schema、canonical 和图像对应。测试已通过且没有新修改时不反复重跑。输出 A/B 全编号关闭清单、未测项、两仓库提交/分支、迁移与回滚说明、部署顺序；修复交付不等于已部署，未经明确要求不修改生产。

总提示词结束

================================================================

通用执行约束
请实际修改代码并验证，不仅输出建议。先读取仓库 AGENTS.md、Git 状态与当前实现，若问题已修复则用测试证据关闭，不重复改动。保护用户改动和历史记录。
保持精确文章机会审批、研究与商业隔离、WordPress draft-only、非草稿保护和项目既有素材授权政策。不得通过关校验、降门槛、标所有来源官方或删失败数据来取得通过。
测试使用内存/临时数据库、mock 或明确测试 WordPress，不触碰生产库或偷偷批量调用付费模型。真实外部验证条件不足时完成本地实现并标 NOT TESTED，记录可执行验证步骤，不伪造通过。
涉及数据库、队列、QA、媒体和契约变更时提供迁移、历史兼容和回滚方案；记录 docs/audit/repair-progress.md 中对应编号、代码位置、测试证据、状态和下一步。不要把未部署的实现说成已上线。
原审计复现脚本断言的是错误行为，修复后应新增正确期望的回归测试，而不是继续维持原缺陷断言为绿。


================================================================

A01 提示词开始

A01 修复前端契约序列化损坏

请修复 A01，优先级 P1。责任范围：前端主修 CMS 验证。

审计证据：已复现。已观察的问题：heading 的源 enum 为 [2,3]，生成物变为 [{},{}]，合法 H2/H3 被拒绝；comparison_table 示例二维数组变为带 Count 和 SyncRoot 的对象。

首先检查：前端 scripts/generate-component-catalog.ps1；contracts/*.json；主题 content-contract/component-registry.v1.json 和各 generated 文件。

依赖：无；与 A19 协同。

实现要求：

1. 定位生成器对 PowerShell 数字、数组、PSCustomObject/包装对象的类型处理错误。在发布实际使用的 PowerShell 版本复现并修复，优先避免自制不完整序列化；维持可重复生成与现有校验和约定。

2. 从源 Registry 重新生成公开契约、主题内生成物和发布包 Schema。禁止只手改生成 JSON，禁止把 heading enum 放宽成任意值。

3. 增加源 schema 与生成 schema 的语义一致性检查；每个 page_block 的示例必须同时通过组件 Schema、Page Schema 和可用的 PHP 验证。对 presentation_meta 使用其自己的接口验证。

验收要求：H2=2、H3=3 通过，其他不支持层级拒绝；table.rows 保持二维数组；连续生成两次产物一致；契约 checksum 对齐；原有文章兼容。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A01 提示词结束

================================================================

A02 提示词开始

A02 增加模型提供商 Schema 适配

请修复 A02，优先级 P1。责任范围：CMS。

审计证据：静态确认 真实请求待验。已观察的问题：完整前端 JSON Schema 直接传给 Vertex responseSchema，未区分提供商支持子集；真实线上失败率尚未验证。

首先检查：src/ai/content-engine.mjs；src/ai/vertex-gemini-client.mjs；src/ai/kimi-client.mjs；src/frontend-contract.mjs。

依赖：A01 A19。

实现要求：

1. 核对当前官方 Vertex 与 Kimi 文档，建立 provider-specific schema adapter。完整前端契约继续作为最终权威校验，不因模型限制删掉发布约束。

2. 对 oneOf/const/联合类型/可选字段等逐项处理并记录转换结果。必要时把选组件与组件数据生成拆分；不静默声称等价转换。保留 metadata 的合法输出结构。

3. 客户端解析 JSON 后做任务级 Schema 校验；将错误路径和约束反馈给有次数上限的修复请求。永久不兼容错误停止原样重试，输出可诊断代码。

4. 增加最小真实请求验证脚本，仅在凭据与授权可用时运行；保存脱敏请求结构、模型、日期和结果。无额度也必须完成 mock 与契约回归，不宣布真实调用通过。

验收要求：提供商请求符合其当前支持范围；不支持的约束有明确处理；畸形 JSON、Schema 不匹配、限流、截断均有预期结果；最终页面仍按原契约校验。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A02 提示词结束

================================================================

A03 提示词开始

A03 按文章主题限定证据就绪范围

请修复 A03，优先级 P1。责任范围：CMS。

审计证据：核心判定已复现。已观察的问题：Opportunity 采用城市全量事实与城市家族数，故宫文章可借颐和园事实判 ready。

首先检查：src/repository.mjs 中 upsertContentOpportunity、rebuildCoverageMatrices、getTopicPackage；src/research-strategy.mjs。

依赖：A18 A14。

实现要求：

1. 建立 opportunity 级证据选择：实体、地域、关系、搜索意图、内容类型与适用时间。城市背景允许显式引用，但不能替代目标实体的必需槽位。

2. Coverage 每个槽位保存支持 Claim、Evidence Span、Source Family 和匹配原因。来源家族数只基于本机会真实使用证据。无关景点冲突不得阻断本篇。

3. Planner/Writer 接收限定证据包；在证据变更后重新计算并定向恢复审批机会。保留精准机会审批，避免同城市多个机会互相借用审批或修改身份。

验收要求：故宫机会仅有颐和园事实必须不 ready；补齐故宫证据后 ready；无关冲突不阻断；相关文章证据变 stale 后撤回 readiness；两个无关来源不能凑独立数。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A03 提示词结束

================================================================

A04 提示词开始

A04 让多模态覆盖审计检查真实证据

请修复 A04，优先级 P1。责任范围：CMS。

审计证据：审计输入已复现。已观察的问题：抽取使用图片视频，但 coverage audit 只有文本和 Claims，无法识别视觉遗漏。

首先检查：src/ai/kimi.mjs 中 auditCoverage 和 buildCoverageInput；src/repository.mjs 分段与覆盖记录；src/pipeline.mjs。

依赖：A13 的定位设计可协同。

实现要求：

1. 给覆盖审计传入与抽取对应的原始图片/视频范围或可追溯且经过完整性检查的 OCR/转录缓存，绑定 asset ID、页码或时间段。

2. 保存 expected/received 模态与资源数量、成功失败状态；下载失败、模态不支持、音视频缺失必须进入 missing_evidence 或明确降级状态，不能把空 gaps 当成完整覆盖。

3. 定向重试只补缺口并重新审计同一证据；缓存与重试可复用输入，避免重复上传全视频。明确无事实图片与无法读取图片的区别。

验收要求：图片独有预约要求被遗漏时发现 gap；图片 404、空视频、文本为空不能自动通过；真实无关装饰图有可解释结果；补齐后才 finalize。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A04 提示词结束

================================================================

A05 提示词开始

A05 把硬性 QA 绑定最终可见页面

请修复 A05，优先级 P1。责任范围：两个仓库。

审计证据：空页面与空台账已复现。已观察的问题：确定性门禁主要看 Markdown，最终 blocks 可为空，证据台账为空也不必然被拦截。

首先检查：src/ai/content-engine.mjs 中 applyDeterministicGates；src/frontend-contract.mjs；src/publish-page.mjs；前端 Page Schema 与渲染器。

依赖：A06 A03 A19。

实现要求：

1. 以最终 editorial page 作为审核正文，确定性提取可见文本、标题、FAQ、图片和链接。模型 reviewer 明确逐 block 核对证据与遗漏，不只审原 Markdown。

2. 建立稳定 block ID 与 Claim/Source/Span 映射；校验事实块覆盖、引用来源关系、过期与冲突披露。纯建议允许明确标记为非事实；不能仅通过非空数组或关键词凑数。

3. 禁止空正文，验证关键内容从 draft 到 page 的保留与 FAQ/schema 可见一致性；将 Research 污染检查应用到最终 editorial blocks，Commercial overlay 另做安全与披露检查。

4. 优先减少第二次模型改写，通过确定性映射复用已审核文案。完成 A06 的版本绑定后，发布只接受审核过的 exact page hash。

验收要求：空 blocks、空事实台账、虚构 source_id、丢失关键例外、新增无证据价格、仅最终页含商业污染均拒绝；合法非事实建议和已隔离商业层可通过。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A05 提示词结束

================================================================

A06 提示词开始

A06 隔离文章修订与旧 QA 结果

请修复 A06，优先级 P1。责任范围：CMS。

审计证据：已复现。已观察的问题：saveDraft 增加 revision 但读取 review 不校验版本，重写后仍能取得旧 passed=true。

首先检查：src/repository.mjs 中 saveDraft、getDraftPackage、saveReview、下游 composition；src/pipeline.mjs 发布与重试阶段；src/db.mjs。

依赖：A16；为 A05 A11 提供版本基础。

实现要求：

1. 给 QA、Page、Commercial、Publish 及任务绑定 draft revision、内容/页面 hash 和证据快照版本；定义准确的当前版本选择规则。

2. 文章改写时原子失效所有派生结果；执行旧任务前拒绝版本不符，避免在途模型返回覆盖新修订。更新 retryContent 与启动恢复逻辑。

3. 为历史数据迁移，无法证明对应当前版本的历史 review 标为需重审；保留历史审计记录，不能全部标通过。使用事务或 compare-and-swap 防竞争。

验收要求：审核通过后改稿，再执行旧商业/发布任务，必须在外部写入前拒绝；新稿重审后可交付；在途旧响应不能覆盖新稿；历史记录可追溯。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A06 提示词结束

================================================================

A07 提示词开始

A07 补齐配图重试与非插画任务状态

请修复 A07，优先级 P2。责任范围：CMS。

审计证据：失败选择已复现 其余静态确认。已观察的问题：failed 图片不被 plannedVisuals 选中；地图、信息图和实景搜索缺少完整执行器。

首先检查：src/repository.mjs 中 plannedVisuals、failVisual；src/pipeline.mjs generate_visuals；src/visuals/vertex-imagen.mjs。

依赖：A06 A08。

实现要求：

1. 为图片设计独立任务与可重试状态，区分 planned/running/retryable_failed/permanent_failed/completed/explicitly_omitted。现有数据迁移不丢失败原因。

2. 文章定义必需和可选素材；必需素材未完成不交付，可选素材仅通过明确降级记录省略。实际完成数与页面引用参与最终 gate。

3. 对 search_real_image、render_map、render_infographic 建立真实执行接口或明确待处理任务。没有配置时不可伪称已渲染；地图只使用验证过坐标，信息图只使用结构化事实，模型仅生成非事实插画。

验收要求：临时 503 后真正重试同一图；永久失败有明确状态；空素材不默默成功；必需图缺失阻断；可选降级可解释；地图/信息图路径有可验证产物或明确未完成。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A07 提示词结束

================================================================

A08 提示词开始

A08 持久化单张媒体进度并复用素材

请修复 A08，优先级 P2。责任范围：CMS 与 WordPress 适配。

审计证据：部分失败已复现。已观察的问题：一批媒体全部上传后才保存 ID，部分成功丢进度；修稿删除所有视觉记录导致重复生成上传。

首先检查：src/wordpress.mjs 中 resolveVisualMedia、uploadMedia；src/pipeline.mjs uploadVisualMedia；src/repository.mjs replaceDraftVisuals。

依赖：A16 A06。

实现要求：

1. 每张媒体上传成功立即持久化 WordPress ID、站点、URL、输入/文件 hash 和状态；任务重试按已保存结果续跑。

2. 按 asset hash、站点和 rendition 建立复用身份；修稿比较视觉输入而非全部删除，保留未变化素材的生成文件及 WordPress 映射。不同站点不得误复用同一个数值媒体 ID。

3. 对响应丢失等不确定结果设计对账或幂等机制，并保留孤儿资源追踪与人工审阅清理列表；不得在重试中自动删除可能已被文章使用的媒体。

验收要求：第一张成功第二张失败后仅重试第二张；纯文案修订不重复生成/上传；图像输入变化会更新；站点切换不混淆；响应丢失不会无限重复资源。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A08 提示词结束

================================================================

A09 提示词开始

A09 修复事实时效与引用真实性

请修复 A09，优先级 P1。责任范围：CMS。

审计证据：已复现。已观察的问题：抓取时间代替事实时间；旧官方证据可借新 UGC 时间取得当前待遇；伪造 source_quote 可写入证据。

首先检查：src/repository.mjs 中 saveCapture、saveExtraction、classifyFreshness、hasCurrentOfficialEvidence；Claim 与 Evidence Span 数据模型。

依赖：A04 A18。

实现要求：

1. 明确 captured_at、published_at、observed_at、verified_at 的语义与可信程度。重复抓取不刷新事实验证时间；未知或未来异常时间必须保守处理。

2. 按同一证据行计算官方性、时效和适用条件，禁止把任意官方行与别处的新时间拼接。不同事实类别可配置 TTL，但不可用全局最新抓取时间覆盖。

3. 文本引用保存可验证 offset 或受控规范化匹配；不存在原文的 quote 进入无效/待复核，不能进入已支持 KB。图像视频绑定真实 OCR/转录、资产和时空位置。

4. 迁移历史数据保留原值并标记时间/引用可信状态；对受影响文章产生重审或更新任务。

验收要求：2020 年票价今天导入仍旧；同内容再抓取不变新；旧官网加新 UGC 不成为当前官方；虚构 quote 拒绝；正常中文标点规范化不误杀；视觉引用有来源定位。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A09 提示词结束

================================================================

A10 提示词开始

A10 按实体和画面匹配实景图

请修复 A10，优先级 P1。责任范围：CMS。

审计证据：静态确认。已观察的问题：来源内前若干图按顺序分配给文章，未按图片实体匹配；插画转实景后 alt 可能不对应。

首先检查：src/repository.mjs 中 authorizedSourceAssetsForBrief、normalizeVisuals；Source Asset 与 Evidence Span；媒体元数据。

依赖：A03 A04 A07 A08。

实现要求：

1. 素材建立实体、地点、画面描述、Claim/Span 关联、匹配分数与来源；选择应按目标槽位 image_subject 与真实画面，而非笔记位置或取模轮换。

2. 禁止把文章证据来自某笔记等同于该笔记所有照片都匹配。无匹配实景图时走 A07 的缺失或明确降级，不把餐厅图标成景点图。

3. 基于选中的真实素材生成准确 alt/caption，不继承插画描述；不强制把首个插画槽改成不对应的实景图。把已授权的人工导入实景照片纳入统一池。

4. 保留已有授权政策与素材追溯；同一图片重复使用有显式理由，不循环填充假装不同场景。

验收要求：故宫 Claim 所在混合笔记的餐厅封面不能成为故宫实景图；正确图片被选中且 alt 相符；缺图不捏造；人工实景图可选；授权来源记录不丢失。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A10 提示词结束

================================================================

A11 提示词开始

A11 统一可见内容与 SEO 结构化数据

请修复 A11，优先级 P2。责任范围：两个仓库。

审计证据：静态确认 线上输出待验。已观察的问题：Draft、Page、SEO 和 JSON-LD 可不一致，WordPress 编辑不更新 schema；canonical 和 robots 私有字段未完整消费。

首先检查：src/repository.mjs 中 draftMetadata、buildArticleSchema；src/publish-page.mjs；前端 inc/cms-articles.php 及 SEO 输出 hooks。

依赖：A05 A06 A17。

实现要求：

1. 统一 pageId、title、slug 和页面版本，FAQ/schema 从最终可见 blocks 派生；WordPress 保存和编辑后重新生成或可靠失效结构化数据。

2. 以 WordPress 最终 permalink、实际 slug、发布/修改时间更新 canonical 和 Article，处理非默认固定链接与重复 slug。保留 WordPress 核心 canonical，明确私有 robots/canonical 字段的受控用途。

3. 补齐适用的 OG/Twitter 元数据、真实图片引用、真实作者/组织与审核信息；不得捏造个人作者。探测当前 SEO 插件的输出归属，避免重复 title/description/schema。

4. 增加最终 HTML 自动验证，确保可见 FAQ 与 schema 一致、schema 不含内部 ID、草稿仍受 WordPress 访问约束。

验收要求：改标题/FAQ 后 schema 同步；slug 冲突和日期型 permalink 正确；核心或插件不产生重复冲突 metadata；OG 图可访问且对应文章；未发表稿不被擅自公开。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A11 提示词结束

================================================================

A12 提示词开始

A12 对齐 CMS 与 PHP 校验边界

请修复 A12，优先级 P2。责任范围：两个仓库。

审计证据：CMS 已复现 PHP 静态确认。已观察的问题：CMS 接受部分 HTML/URI/组件数据，PHP 后期拒绝；未证明公开 XSS 可利用。

首先检查：src/frontend-contract.mjs validateJsonSchema；前端 inc/cms-articles.php 验证函数与 wp_kses。

依赖：A01 A19。

实现要求：

1. 建立共享正反例集与两端支持的 Schema 子集，逐项对齐 HTML、URI、日期、heading level/variant、列表、图片 role、表格列数及商业 allowlist。

2. 将能在 CMS 判断的约束前移；保持 PHP 权威验证和安全转义，不能为消除拒绝而放宽前端。WordPress 媒体存在性仍由服务端检查。

3. 提供受认证且不写入的 WordPress dry-run 校验入口或等效测试 harness；返回稳定 code/path。将反馈交给 A02 的有界结构化修复逻辑。

验收要求：script/event handler/危险协议及错列数两端均拒绝；合法 inline HTML 不被误拒；整数和空对象等边界一致；dry-run 不创建 post/media/term；无需把 mock 接受误报为线上 XSS。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A12 提示词结束

================================================================

A13 提示词开始

A13 补齐长文本与 PDF 视频分段

请修复 A13，优先级 P2。责任范围：CMS。

审计证据：长单段已复现 其余静态确认。已观察的问题：130000 字符单段不拆分，提取与覆盖都截断 120000；PDF/视频缺少完整页与时间分段。

首先检查：src/research-strategy.mjs segmentSource、splitText、splitPdfText；src/ai/kimi.mjs；src/adapters/manual-source.mjs；Vertex 视频输入。

依赖：A04 协同。

实现要求：

1. 对超长单段实行硬 token/字符上限、边界切分与可追溯 overlap，段记录原文起止偏移；消除抽取和审计共享静默截断造成的盲区。

2. PDF 保留页码、提取状态和内嵌图片/扫描页；可配置 OCR/逐页视觉兜底，没有能力时明确缺失并进入处理任务。

3. 视频按时长与 token 预算划分明确 start/end 片段；转录与关键帧可复用，避免每个文本段反复提交整个 YouTube 视频。上传限额与模型真实限额协调。

4. 建立模态支持矩阵与可观察的预检，不将文件格式支持等同于所有证据已读取。

验收要求：超长单段尾部事实仍可提取且定位；overlap 不重复 Claim；PDF 多页/扫描页保持覆盖状态；长视频分段范围完整无重复全量输入；不支持格式清晰报错。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A13 提示词结束

================================================================

A14 提示词开始

A14 纠正来源家族与独立佐证计数

请修复 A14，优先级 P2。责任范围：CMS。

审计证据：中文样例已复现 计数静态确认。已观察的问题：中文轻微改写可被分成独立家族；corroborated 按 Claim 行数而非独立来源计算。

首先检查：src/research-strategy.mjs classifySourceFamily；src/repository.mjs analyzeSourceFamily、rebuildKnowledge；source_family 数据。

依赖：A09；A03 使用其结果。

实现要求：

1. 针对中文加入字符 n-gram/近似重复特征，结合作者、转载引用、时间、文本和图像 hash；不要仅按词分隔集合判断。

2. 家族内派生内容可以有增量事实，但不能重复增加同一事实的独立支持数。corroborated/support_count 使用真正独立家族及相同适用范围，不使用 rows.length。

3. 相似度只能识别重复或提出候选，不能自动消除真实否定、价格差异或冲突。历史家族重算需审计记录，更新受影响机会和 QA 证据版本。

验收要求：需要预约/必须预约等近重复不算双独立；同来源两条相同事实不 corroborated；两个独立权威来源可佐证；否定或不同季节事实不被误合并。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A14 提示词结束

================================================================

A15 提示词开始

A15 实现会话撤销与登录限速

请修复 A15，优先级 P2。责任范围：CMS。

审计证据：旧会话已复现 限速静态确认。已观察的问题：修改密码后旧 Cookie 仍有效至到期；登录无限速且 scryptSync 占主事件循环。

首先检查：src/auth.mjs；src/server.mjs 认证路由；src/db.mjs 用户会话迁移。

依赖：无。

实现要求：

1. 实现 sessionVersion/passwordChangedAt 或服务端会话表，密码修改后撤销所有旧会话并签发新会话；logout 撤销当前会话，保留 Secure/HttpOnly/SameSite。

2. 登录按账号和来源做有界限速、退避与防枚举响应；可信代理/IP 提取不可盲信任任意 X-Forwarded-For。不要依赖假定存在的 Cloudflare 规则替代应用行为。

3. 使用异步密码计算或受控 worker 并发，保持抗时序处理和密码散列安全；迁移旧会话保守失效，不输出凭据或 Cookie 到日志。

验收要求：改密前 Cookie 立即失效，新 Cookie 可用；logout 后当前旧 Cookie 失效；错误登录受限，合法登录可恢复；API/worker 在密码计算压力下仍响应。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A15 提示词结束

================================================================

A16 提示词开始

A16 修复队列租约和诊断副作用

请修复 A16，优先级 P1。责任范围：CMS。

审计证据：已复现。已观察的问题：构造第二个 Repository 会把仍运行的任务重置为 queued，status/sync CLI 可能触发。

首先检查：src/repository.mjs 构造器、claimJob；scripts/check-frontend-contract.mjs；scripts/sync-frontend-contract.mjs；Pipeline 启停。

依赖：先执行 为可靠性基础。

实现要求：

1. 删除 Repository 构造器恢复任务的副作用。采用明确 worker owner、lease expiry、heartbeat 与过期回收；诊断命令只读，不创建或迁移生产库。

2. claim/complete/fail 使用 owner 和任务版本条件更新，旧 worker 不能完成已被接管的租约。合理选择 lease/续租间隔覆盖长模型请求并处理进程退出。

3. 恢复只针对确认过期的任务；不要把 running attempts 全清零导致无限重试。同步契约 CLI 若写入契约，也不能重置不相关任务锁。

4. 保留单进程默认行为；租约正确性验证后才扩展并发。

验收要求：运行时执行 status 或构造 Repository 不改锁；崩溃过期可恢复；活跃 worker 不被抢；旧 owner 不能提交结果；并发 claim 只有一个获胜；读命令不创建新 DB。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A16 提示词结束

================================================================

A17 提示词开始

A17 消除 WordPress 草稿幂等写入窗口

请修复 A17，优先级 P2。责任范围：前端与 CMS 交付适配。

审计证据：静态确认 故障注入待验。已观察的问题：post 先创建，幂等 postmeta 后写；中断或并发首次 POST 可能产生重复草稿。

首先检查：前端 inc/cms-articles.php 中 find_mapped_post_id、resolve_target_post_id、upsert_article；CMS WordPress 适配器。

依赖：A16 A06 A12。

实现要求：

1. 先用本地 WordPress 测试环境验证插入后中断与并发窗口。设计稳定 cms_draft_id/pageId 的唯一幂等记录、互斥或事务恢复机制，考虑 WordPress hooks 和外部副作用不能简单回滚。

2. 记录写入阶段与已分配 post ID；taxonomy/metadata 失败后重试恢复同一 post，响应丢失可对账。避免只依赖无唯一约束的 postmeta 查找。

3. 保持鉴权、POST/PUT ID 一致、draft-only 和非草稿保护；幂等重试不可覆盖已发布文章或其他 CMS page。保留可诊断错误和恢复流程。

验收要求：并发两次首次 POST 只一个 post；post 插入后故障重试仍同 ID；taxonomy 失败可恢复；响应丢失无重复；已经 publish 的 post 拒绝覆盖；权限边界不变。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A17 提示词结束

================================================================

A18 提示词开始

A18 补齐官方证据核验操作闭环

请修复 A18，优先级 P1。责任范围：CMS。

审计证据：静态确认 默认值已复现。已观察的问题：来源默认 authority_level=4，门槛要求官方但 API/UI 无正常设置入口；集成测试通过直接 SQL 设置 1 绕过缺失步骤。

首先检查：src/db.mjs authority 与 verified 字段；src/repository.mjs saveCapture、rebuildKnowledge；src/server.mjs；frontend/src 管理界面。

依赖：A09 时间语义协同。

实现要求：

1. 实现来源权威与事实核验管理 API/UI，展示依据、来源 URL、核验人、时间、适用范围和历史记录；官方域名识别仅作为候选，不能允许上传方自行宣称官方。

2. 为合法官网导入建立可完成确认的流程；明确来源可信度与具体 Claim 已核验是两件事。未知或 UGC 默认保持保守，不整体放宽 requires_official。

3. 核验或撤销后定向重建知识、机会 Coverage，恢复或暂停受影响的已批准机会；不制造新审批、不要求改数据库。

4. 把直接 SQL 升级来源的集成 fixture 替换或补充为真实 API 操作路径。

验收要求：导入官网→管理员确认→核验事实→等待机会恢复；非管理员不能标官方；UGC 不自动变官方；过期官方证据不通过；撤销有追溯并重新计算。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A18 提示词结束

================================================================

A19 提示词开始

A19 区分正文组件与页面设置接口

请修复 A19，优先级 P1。责任范围：CMS 与前端契约。

审计证据：已复现。已观察的问题：CMS 丢弃 interface，presentation_meta 可以通过 blocks 计划校验但被最终 Page Schema 拒绝。

首先检查：src/frontend-contract.mjs normalizeComponent、capabilities、validateCompositionPlan；src/ai/content-engine.mjs promptCapabilities；前端 Registry。

依赖：A01。

实现要求：

1. 保留 interface、cmsUsable、renderMode 和生命周期状态；正文候选与计划只接受 page_block。presentation_meta 通过明确 schema 写入 metadata.presentation。

2. 模型提示与输出 DTO 清楚区分两类能力；不让 article_hero/share_this_page/table_of_contents 出现在 blocks。计划阶段提前校验类别与 variant。

3. 始终提供适用的基础 paragraph/heading/list/image 能力，不因语义关键词未命中而丢失必要构件；仍禁止不存在、弃用或未获支持的组件。

验收要求：三项 presentation 设置可正确保存到 metadata；放进 blocks 在 plan 即拒绝；原先 article_hero 计划通过样例改为失败；合法正文与历史兼容路径正常。

交付：实现、针对性回归、迁移/兼容说明、对应编号状态与未测项。不得仅交付方案。

A19 提示词结束

================================================================

B 类优化提示词

与 A 类同样受通用约束限制；以改进收益和回归证据验收，不把策略建议误报为已利用漏洞。

================================================================

B01 按内容任务决定篇幅和配图 提示词开始

请替换统一最低 800 词和强制 FAQ/字数配图策略，建立按内容类型、读者任务与证据充分性判定的质量配置。对短实用答案、步骤、攻略、对比设置不同充分性标准；可选 FAQ 不能以删除关键信息换取通过。同步 Writer、QA、素材计划和策略文档，更新策略版本并保持历史版本可追溯。验收：简短但证据完整的问答通过，冗长重复稿仍失败；正文长度变化不造成素材计划数量矛盾。

B01 提示词结束

================================================================

B02 增加读者可核实的出处和适用条件 提示词开始

请把内部证据映射转换为读者可理解的官方出处、核验日期、条件与例外；为价格、预约、支付和安全信息提供恰当来源链接。保留 source ID 内部追踪但不泄漏研究元数据。使用真实作者/组织与审核信息，不能虚构经历。与 A09/A11 的证据日期和页面输出共用数据。验收：关键事实可追至真实 URL 和证据版本，原文例外未丢失，内部 src/claim 标识不进入可见页面。

B02 提示词结束

================================================================

B03 校正 GEO 与 FAQ 搜索策略 提示词开始

请核对修复当天 Google 官方 AI 搜索与结构化数据文档，修订 GEO/SEO 规则，去除 FAQ 富结果、固定字数、llms.txt 或 schema 必然带来引用排名等承诺。原审计核对的官方记录说明 FAQ rich result 自 2026-05-07 起不再展示；执行时重新核对并记录来源日期。FAQ 仅在满足真实问题时生成，结构化数据与可见内容一致。验收：提示词和质量门禁不再为不存在的搜索特性强制填充内容，也不删除有用 FAQ。

B03 提示词结束

================================================================

B04 支持已有文章更新合并和退役 提示词开始

请将内容机会区分 create/update/merge/retire，建立 Claim→文章→已发布 URL 的反向影响索引。新增证据与已有文章重叠时可创建定向更新建议，不仅因标题相似而 dismissed。更新发布策略继续尊重已发表文章保护：默认产生更新草稿/差异供审核，不能直接覆盖已发布正文。验收：票价变化产生指向正确文章的更新机会，重复来源不会重复建稿，update 审批与 create 身份清楚，退役只提出受控建议。

B04 提示词结束

================================================================

B05 稳定内容块与真实内部链接 提示词开始

请用稳定 block ID、语义角色、来源和修订记录表达页面内容，优先确定性映射已审核文案。内部链接仅从已发布页面库存解析，按实体、意图、目标可用性与自然锚文本选择；不要让模型猜 URL。处理草稿链接、404、重定向、重复锚点和文章自身链接。验收：重排不丢证据身份，相关链接可解析且不指向测试或未发布页；与 Commercial 定位索引不冲突。

B05 提示词结束

================================================================

B06 在租约安全后增加有限并发 提示词开始

请在 A16 和版本门禁完成后，为任务 DAG 提供按 provider 受限并发、独立图片队列与公平调度。保持同实体同修订写入串行；长模型任务不能阻塞健康检查或全部短任务。支持可配置默认保守并发、背压、停止和恢复。验收：模拟慢请求期间其他独立任务可前进；同一阶段不重复领取；限流不形成重试风暴；并发提高后的费用和资源使用有测量。

B06 提示词结束

================================================================

B07 记录模型成本并按任务分配预算 提示词开始

请记录每阶段模型、prompt/schema 版本、input/output/cached token、耗时、重试、估算费用和最终 QA 结果。usage 缺失记 unknown，不能记零。价格表来源和时间可配置，费用仅标估算；不假设各提供商相同。提供按每篇 QA 合格文章聚合的成本与延迟，并允许抽取、规划、写作、审阅分阶段配置模型/思考预算。验收：从 mock usage 可正确汇总重试费用，无密钥/全文进入日志；模型切换有评测依据，不强制换更贵模型。

B07 提示词结束

================================================================

B08 缩小提示上下文并缓存稳定阶段 提示词开始

请建立阶段专用白名单 DTO，去掉 raw JSON 与解码对象重复传输、无关城市事实、旧 review/publish/commercial 记录。Research prompt 不应接收历史商业组成。按 source/证据 hash、模型、prompt version、schema version 和策略版本构建缓存键，明确变更失效与失败不缓存策略；图片/视频处理缓存同样追溯。验收：同输入重跑减少调用，事实/策略变更命中失效，文章间或来源间不串缓存，审计可看到输入规模与命中率。

B08 提示词结束

================================================================

B09 按错误类型恢复任务和契约 提示词开始

请统一结构化错误分类：429/503 使用 Retry-After、指数退避和 jitter，401/403 与确定性 Schema 错误停在需处理状态，格式错误有界修复，资源缺失走补证/素材任务。契约同步成功后定向恢复受 stale_contract 阻塞的页面并重新校验当前版本；检查 snapshot 身份是否只依赖 Registry checksum 而遗漏 Page/Publish Schema 变化，如存在则修复不可变 provenance。验收：永久错误不烧完多次相同调用；契约更新不会永久卡死或无限重编排；旧快照可追溯；失败原因可操作。

B09 提示词结束

================================================================

B10 建立跨仓库最终产物验收门禁 提示词开始

请把两个仓库的真实生成契约、Schema 转换、版本 QA、故障恢复、WordPress 保存编辑渲染串成隔离验收。至少覆盖真实 Registry 样例、模型错误输出、缺模态、缺图、响应丢失、并发幂等、编辑后 FAQ/schema 与 canonical。mock 可验证调度但不能冒充真实模型/PHP/浏览器结果；提供有环境时可执行的最小真实 smoke。验收输出区分 PASS/FAIL/NOT TESTED，记录代码版本和命令。真实外部测试缺凭据时标已实现待外部验证，不能整体报全部完成。

B10 提示词结束

================================================================

最终验收提示词开始

请对本次 SoloToChina 两仓库修复做最终验收，读取 docs/audit/repair-progress.md、原审计和本交接文档，逐项核对 A01-A19、B01-B10。不要只引用已有测试通过结论；将每项映射到实际实现、回归测试、生成产物、数据库迁移和必要的外部验证。

重点尝试原缺陷的反例：数字 enum/二维数组；presentation 进入 blocks；跨景点假就绪；无法读取的图片覆盖；旧票价新抓取；虚构引用；空最终页；旧 QA 发布；配图失败续跑；部分媒体成功；活跃 worker 被 CLI 抢锁；首次 POST 并发/响应丢失；改密旧 Cookie；WordPress 编辑后 schema 漂移。所有故障注入在隔离环境执行。

运行两仓库适用的现有验证和新增回归，输出逐项状态表、命令与结果、未测项原因和下一步命令。没有真实模型/WordPress/浏览器环境的部分明确 NOT TESTED，不把 mock 结果升级成线上证据。若发现可以独立修复的剩余问题，继续修复和验证，不提前结束。

提供发布清单：两仓库版本与兼容矩阵、生成契约校验和、迁移前备份、历史 QA/任务/素材状态处理、部署顺序、回滚兼容性、首次上线 smoke 和观测指标。破坏性迁移不得只依赖代码回滚。除非已有明确部署授权，仅交付可审查结果，不对生产执行。

在结束前保存可供下一账号续接的状态：当前分支/提交、工作区改动、已完成编号、待验证编号、阻塞原因和下一条可执行命令。最终报告不将优化建议称为已发生漏洞，也不把未发现漏洞表述为系统安全保证。

最终验收提示词结束
